{
  "name": "images-hunter-api",
  "version": "1.0.0",
  "description": "rest apis to index and find images by their metadata",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --testPathIgnorePatterns='/tests/e2e/'",
    "tdd": "jest --testPathIgnorePatterns='/tests/e2e/' --watch",
    "test-ci": "jest --ci --coverage --setupFiles './config/jest.config.js'"
  },
  "author": "Wassel ALAZHAR",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.2",
    "dotenv": "^5.0.0",
    "express": "^4.16.2",
    "mongoose": "^5.0.1"
  },
  "devDependencies": {
    "jest": "^22.1.4",
    "sinon": "^4.2.2",
    "supertest": "^3.0.0"
  },
  "jest": {
    "coveragePathIgnorePatterns": ["/config/"]
  }
}